services:
  postgres:
    image: "postgres:16-alpine"
    environment:
      POSTGRES_DB: "ticketly"
      POSTGRES_USER: "ticketly"
      POSTGRES_PASSWORD: "ticketly"
    ports:
      - "5432:5432"

